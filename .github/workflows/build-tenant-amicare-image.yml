name: build-tenant-amicare-image

on:
  push:
    branches: [main]
    paths:
      - "sites/ami-care/**"
      - ".github/workflows/build-tenant-amicare-image.yml"
  workflow_dispatch: {}

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: optidigi/siab-platform-site-ami-care

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-,format=short
            type=sha,prefix=sha-,format=long

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: sites/ami-care
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=tenant-amicare
          cache-to: type=gha,mode=max,scope=tenant-amicare
          build-args: |
            SITE_URL=https://ami-care.nl

      - name: Smoke-start pushed image
        env:
          IMAGE_REF: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:sha-${{ github.sha }}
        run: |
          set -euo pipefail
          container_name="ami-care-smoke-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          cleanup() {
            docker logs "$container_name" > smoke-container.log 2>&1 || true
            docker rm -f "$container_name" >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          docker pull "$IMAGE_REF"
          docker run --detach \
            --name "$container_name" \
            --publish 4321:4321 \
            --tmpfs /data:mode=1777 \
            --env CMS_DATA_DIR=/data \
            --env SITE_URL=https://ami-care.nl \
            "$IMAGE_REF"

          deadline=$((SECONDS + 60))
          until curl --fail --silent --show-error http://127.0.0.1:4321/healthz; do
            if [ "$SECONDS" -ge "$deadline" ]; then
              docker logs "$container_name" || true
              exit 1
            fi
            sleep 3
          done

      - name: Upload smoke-start logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smoke-start-logs
          path: smoke-container.log
          if-no-files-found: ignore
