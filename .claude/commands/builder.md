---
description: Review the future Builder architecture direction
---

Use `docs/decisions/builder-platform.md` and root `AGENTS.md`.

This command is for architecture and guidance only until `apps/builder` is
implemented. Do not route Builder work through legacy `/new-site` or `/add-cms`
unless the operator explicitly asks for those legacy workflows.
