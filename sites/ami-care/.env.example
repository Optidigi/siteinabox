# Name of the external docker network that Traefik
# attaches routed containers to. Find it with:
#   docker network ls
# Look for the network Traefik and routed apps share.
PROXY_NETWORK=proxy

# CMS runtime (read by the SSR server)
CMS_DATA_DIR=/data
