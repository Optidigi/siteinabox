---
import { site } from '../../content/site';
import { SiteHeader } from './SiteHeader';
---

<SiteHeader
  client:load
  nav={site.nav}
  intakeHref={site.links.intake}
  loginHref={site.links.beheer}
/>
