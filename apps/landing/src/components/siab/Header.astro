---
import { site } from '../../content/site';
import { SiteHeader } from './SiteHeader';

interface Props {
  hero?: boolean;
}

const { hero = false } = Astro.props;
---

<SiteHeader
  client:load
  nav={site.nav}
  signupHref={site.links.signup}
  loginHref={site.links.login}
  hero={hero}
/>
