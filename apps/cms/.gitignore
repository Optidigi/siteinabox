# dependencies
node_modules/
.pnpm-store/

# build output
.next/
next-env.d.ts
dist/
dist-runtime/
out/

# env
.env
.env.local
.env.*.local
!.env.example

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# editor
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# brainstorming visual companion artifacts (session-scoped)
.superpowers/

# Claude Code local-only settings (per machine, not shared)
.claude/settings.local.json

# payload generated
src/payload-types.ts
src/app/(payload)/admin/importMap.js

# test artifacts
coverage/
playwright-report/
test-results/
.playwright-mcp/
tmp/

# local projection output (DATA_DIR)
.data-out/
# test-run scratch dirs (DATA_DIR override in tests/setup.ts)
.data-test-*/

# typescript incremental build cache
*.tsbuildinfo

# Operator audit screenshots — kept at repo root, never tracked
/*.png
