# Not used by production image builds (repo-root context + root .dockerignore).
# Keep in sync so a mistaken `docker build` from apps/cms still excludes tenant data.
node_modules
.next
dist-runtime
.git
.github
.env
.env.local
.env.test
docs
tests
.superpowers
.claude
README.md
.data-out
.data-test-*
.data
playwright-report
test-results
*.tsbuildinfo
