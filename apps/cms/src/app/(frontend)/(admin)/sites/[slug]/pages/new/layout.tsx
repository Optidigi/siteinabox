import "@/styles/generated-site-renderer.css"
import "@/styles/site-renderer-canvas.css"

export default function NewSitePageEditorLayout({ children }: { children: React.ReactNode }) {
  return children
}
